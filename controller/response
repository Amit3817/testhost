const axios=require("axios")

const result=async (req,res,next)=>{
    try{

    const user=req.user
    const institute=req.institute
    const array =institute.programs
    var concatenatedString;

   if (array.length > 1) {
   concatenatedString = `${array.slice(0, -1).join(', ')} and ${array.slice(-1)}`;
   } 
  else if (array.length === 1) {
    concatenatedString=array[0];
} 


    const summary=await axios.post(
        process.envsocketq,
        {
          question:`give me details approval process for estblashing ${institute.atype} institute for ${concatenatedString}, the type of institution is ${institute.itype} for undergraduate program and post graduate program respectevely`
        },
        {
          headers: {
            'Content-Type': 'application/json',
          },
        }
      );
      console.log(mlApiResponse.data); 


    {"question":"give me details approval process for estblashing new institute for engineering and technology and computer application MCA, the type of institution is all other institutions for undergraduate program and post graduate program respecteveluy"}

}
}
